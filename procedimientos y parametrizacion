int maxPuntos = 10;

float[] xs = new float[maxPuntos];
float[] ys = new float[maxPuntos];

int indice = 0;     // donde guardar elmproximo junto
int cantidad = 0;   // cuántos puntos válidos hay 

void setup() {
  size(600, 400);
}

void draw() {
  background(255);

  // dibujar los puntos guardados
  for (int i = 0; i < cantidad; i++) {
    ellipse(xs[i], ys[i], 20, 20);
  }
}

void mousePressed() {
  
  xs[indice] = mouseX; //guarda nueva posicion
  ys[indice] = mouseY;

  
  indice = (indice + 1) % maxPuntos;

  // aumenta la cantidad hasta el máximo
  if (cantidad < maxPuntos) {
    cantidad++;
  }
}
