void setup() {
  size(600, 200);
  background(255);
  
  noFill();
  stroke(255, 105, 180); 
  strokeWeight(2);
  
  int x = 80;
  
  for (int i = 1; i <= 5; i++) {
    ellipse(x, height/2, i * 30, i * 30);
    x += 100;
  }
}
